# CloudScope – root env example (for local dev or reference).
# Copy to .env and adjust. Docker Compose sets its own env for api/db.

# ---------------------------------------------------------------------------
# Database (used by API and migrations)
# ---------------------------------------------------------------------------
# Docker: use postgresql://cloudscope:cloudscope@db:5432/cloudscope (api service gets this from compose).
# Local dev (API on host, DB in Docker): use postgresql://cloudscope:cloudscope@localhost:5432/cloudscope
DATABASE_URL=postgresql://cloudscope:cloudscope@localhost:5432/cloudscope

# ---------------------------------------------------------------------------
# Docker API healthcheck (optional; defaults shown)
# ---------------------------------------------------------------------------
# HEALTHCHECK_INTERVAL=60
# HEALTHCHECK_TIMEOUT=15
# HEALTHCHECK_RETRIES=3
# HEALTHCHECK_START_PERIOD=30

# ---------------------------------------------------------------------------
# Redis (resources cache; optional – leave unset or set to false to disable)
# ---------------------------------------------------------------------------
# Docker: api service uses redis://redis:6379/0 from compose.
# Local dev (API on host): use redis://localhost:6379/0 if Redis is running.
# REDIS_URL=redis://localhost:6379/0

# ---------------------------------------------------------------------------
# API (FastAPI / uvicorn) – optional for local runs
# ---------------------------------------------------------------------------
# Run API: cd api && uvicorn main:app --host 0.0.0.0 --port 5001
# No FLASK_* vars – this app uses FastAPI.

# ---------------------------------------------------------------------------
# Frontend (Vite) – optional
# ---------------------------------------------------------------------------
# In frontend/.env: VITE_API_URL= (empty = same-origin, use Vite proxy to 5001).
# For dev: leave empty so npm run dev proxies /api and /health to http://localhost:5001.
# VITE_APP_VERSION=1.0.0
