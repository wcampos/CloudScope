# API base URL - leave empty for same-origin (Docker proxy or Vite dev proxy).
# Set to full origin only if API is on another host/port and no proxy, e.g.:
# VITE_API_URL=http://localhost:5001
VITE_APP_VERSION=1.0.0

# Settings page: health check auto-refresh interval in minutes (default 2).
# VITE_HEALTH_CHECK_INTERVAL_MINUTES=2
