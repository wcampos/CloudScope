# API (FastAPI) – for local runs (e.g. uvicorn main:app --port 5001).
# Docker Compose sets DATABASE_URL for the api service.

# Database – required for API and migrations
# Local dev with DB in Docker: postgresql://cloudscope:cloudscope@localhost:5432/cloudscope
# Inside Docker: postgresql://cloudscope:cloudscope@db:5432/cloudscope
DATABASE_URL=postgresql://cloudscope:cloudscope@localhost:5432/cloudscope
